import { createContext, useContext } from 'react';

export type {{pascalCase name}}ContextValue = {
  // Add your context values here
};

const {{pascalCase name}}Context = createContext<{{pascalCase name}}ContextValue | undefined>(undefined);

export const use{{pascalCase name}}Context = () => {
  const context = useContext({{pascalCase name}}Context);
  if (!context) {
    throw new Error('use{{pascalCase name}}Context must be used within {{pascalCase name}}');
  }
  return context;
};

export const {{pascalCase name}}Provider = {{pascalCase name}}Context.Provider;
